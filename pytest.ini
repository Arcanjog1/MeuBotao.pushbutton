[pytest]
markers =
    slow: roda o solver de verdade (benchmark completo) - pule com -m "not slow"
    ai_team_e2e: spawna `python -m ai_team` como subprocesso - nunca deve rodar dentro do proprio gate do `--mode selftest` (recursao); ver ai_team/cli.py::_scope_pytest_gate_for_selftest
