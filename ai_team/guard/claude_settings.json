{
  "$comment": "Registra o hook de guarda no Claude Code CLI. Passado via --settings. O hook roda ANTES de cada Bash/Write e pode negar o comando. $CLAUDE_PROJECT_DIR e' expandido pelo CLI (verificado), o que torna o hook independente do diretorio de trabalho da invocacao.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Write|Edit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 $CLAUDE_PROJECT_DIR/ai_team/guard/pretooluse_git_guard.py",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
