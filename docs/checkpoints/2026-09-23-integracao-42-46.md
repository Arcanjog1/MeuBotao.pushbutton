# Integração PR #42 (motor final) + PR #46 (UI premium) — candidato único

```json
{
  "date": "2026-09-23",
  "branch": "integration/pr42-ui46-final",
  "base": "55e990d962ed22ae1021f0d335db197607bddda1",
  "head": "89384db96050c36d3d0df0f9600d75acf4373cfd",
  "incorpora": {
    "pr42": "claude/butanta-modulation-physical-fixes @ 93f38851836a861acbb48ce25decf8fc3c1b9850",
    "pr46": "codex/modulation-ui-premium-redesign @ ae4586e0"
  },
  "veredito": "OPCAO A (decisao do usuario, 2026-09-23): regra 48 SEM EXCECAO - peca que ocupa o volume real de abertura NAO e' criada (amarracao inclusive); amarracao rejeitada fica NAO RESOLVIDA com revisao humana; so' erro FATAL bloqueia a RUN. Solver do #42 intacto (engine/ blob-identico; nenhuma funcao de solver alterada, conferido por AST). O que muda e' a MATERIALIZACAO: o que o Revit recebe.",
  "changes": [
    "merge do #42 (sem conflito: main e' ancestral) e do #46 (um unico conflito textual, docs/PROJECT_STATUS.md, reconciliado a' mao).",
    "adaptador pos-#42 na UI: revisao humana (regra 76.1), portoes duros, NO_FUNCTIONAL_JUNCTION como nota tecnica, snapshot real da execucao com run_id/revision.",
    "correcao de layout: a nota do resultado voltou a caber em tres linhas.",
    "aa70d0d (regressao do fluxo do botao): estado da RUN (configuracao da Etapa 1 com dono e re-renderizada), contrato explicito da Etapa 3B e materializacao seletiva por peca. A politica de materializacao desse commit estava ERRADA (criava a amarracao que invadia a porta, com isencao por codigo B34/B54 e pelo compensador da 76.1) - substituida pela OPCAO A abaixo. A frase 'Nenhuma regra fisica tocada' do commit era falsa: a consequencia operacional da regra 48 mudou.",
    "OPCAO A (0641b5b): `structural_bond_role` (papel = razao de encontro do solver + codigo B34/B54; compensador 76.1 e canaleta nunca), `materialization_plan` (toda invasao de abertura e toda colisao viram pulo; amarracao pulada vira BOND_UNRESOLVED), `verify_materialization` (preflight refeito so' com o que fica - invasao/colisao residual e' FATAL), `structurally_resolved` no resultado, MISSING_FAMILY como pulo registrado, `failed_count` por peca; UI: 'nao serao criadas', 'amarracoes NAO resolvidas', bloco ESTADO ESTRUTURAL.",
    "REGRAS: secao 48 ganhou a CONSEQUENCIA OPERACIONAL (principio intacto); notas de conflito na secao 3 e nas duas mencoes antigas de 'gate de lote' da secao 11.",
    "Auditoria independente da OPCAO A (3 auditores + verificacao adversarial, 13 achados, todos reproduzidos) - corrigidos em 89384db: (1) o laudo da regra 48 herdava a tolerancia de ruido de 0,5 cm em Z do solver (ate' 5 mm de peca dentro do vao sem registro) - agora 0,1 cm tambem em Z, com a sobreposicao real em 3D registrada; (2) colisao cuja outra peca ja' saiu nao pula mais uma peca valida nem cria amarracao NAO resolvida falsa; (3) Finalizar nao trava a planta inteira por pulo localizado - a parede afetada fica retida (INCOMPLETE_CREATION), fatal continua bloqueando (beta_finalize_allowed, backend e botao); (4) pulo por IDENTIDADE da peca; (5) registro leva a parede da abertura; (6) contabilidade fecha tambem sem altura de fiada; (7) relatorio: RUN executada com 0 criadas, pendencias sem duplicar, paredes retidas da criacao, valor desconhecido como '-'."
  ],
  "tests": [
    "Suite completa sem tests/regression (arvore = 89384db): 1665 passaram, 1 pulado, 1 falhou - tests/test_perf_trace_stall_sampler.py::test_retencao_nativa_de_gil_dispara_e_despeja_as_pilhas (UnboundLocalError de ctypes no proprio teste, Windows); IDENTICA na main 55e990d (teste e perf_trace.py sem diferenca main..HEAD).",
    "tests/regression: 2 falharam / 146 passaram na branch E na main 55e990d, com as MESMAS mensagens (TP1 JUNCTION_MISSING_BINDING 8->9; TGD V2 compensators 61->62). O solver nao mudou desde essa medicao (engine/ blob-identico; nenhuma funcao solve_*).",
    "OPCAO A + auditoria: tests/test_materializacao_e_estado_da_run.py (casos A..H, 76.1, canaleta, colisao com peca ja' pulada, 4 mm e 3 mm em Z, encoste de 0,05 cm permitido, pecas identicas, Finalizar com retencao por parede, contabilidade sem altura, relatorio) + preflight + criacao atomica + retencao + UI + lote: 170 passaram.",
    "Mutacao (codigo restaurado e sha256 conferido depois): isencao de amarracao, criterio so' por codigo, criterio _is_tie, criterio do aa70d0d, sem o invariante, sem a regra da colisao ja' resolvida, tolerancia Z de 0,5 cm, Finalizar pelo preflight.ok - cada uma derruba os testes correspondentes.",
    "Revit (somente leitura, MCP, butanta testes, 46 paredes, 3 m, depois das correcoes): 8379 planejadas, 11 puladas, 8368 criaveis (fecha); verify_materialization 0/0/0; forca bruta: 0 peca em abertura ativa; 17 vergas de canaleta encostam a 221,0 cm com 3e-12 cm (dentro de 0,1 cm); 12 door_void do solver, 0 materializados."
  ],
  "references": [
    {
      "path": "nuvem/core/ui_state.py"
    },
    {
      "path": "nuvem/core/ui_execution.py"
    },
    {
      "path": "nuvem/core/ui_components.py"
    },
    {
      "path": "nuvem/core/wall_modeling.py"
    },
    {
      "path": "docs/PROJECT_STATUS.md"
    },
    {
      "path": "tests/test_materializacao_e_estado_da_run.py"
    },
    {
      "path": "tests/test_controlled_beta_preflight.py"
    },
    {
      "path": "nuvem/REGRAS_MODULACAO_BLOCOS.md"
    }
  ],
  "pr": "https://github.com/Arcanjog1/MeuBotao.pushbutton/pull/49",
  "objective": "Entregar o motor final do PR #42 e a UI premium do PR #46 como UM candidato unico, sem perder nenhum dos dois lados: fisica do #42 e' autoridade, UI do #46 e' a referencia visual. Nao mergear.",
  "known_failures": [
    "Smoke do BOTAO REAL no Revit com a OPCAO A pendente (clique humano). A medicao no modelo real foi feita SOMENTE LEITURA pelo MCP, sem Transaction.",
    "As duas falhas historicas de tests/regression/test_benchmark_baselines.py (TP1 JUNCTION 8->9, TGD V2 compensators 61->62) continuam - identicas na main, provadas em worktree da propria main.",
    "A Etapa 3B da secao 66 (microajuste de abertura) nao e' disparada pelo fluxo do botao; a UI mostra 'nao avaliado nesta execucao'.",
    "Os '371 bloqueios' da execucao do usuario (maquina CIVIX) nao foram reproduzidos aqui: a mesma planta, 3 m, deu 11 invasoes de vao no preflight. Falta o log daquela execucao.",
    "MODO ONLINE (sem beta) nao roda o preflight: continua a regra revista de 2026-08-26 (cria a peca e marca em vermelho). Inalterado em relacao a' main; a secao 48 rege o BETA CONTROLADO. Pendencia para decisao do usuario.",
    "Loader online (`_sync_core_package`) baixa por NOME de branch, nao registra o SHA e cai no cache em silencio: nesse modo nenhum PC prova qual commit roda. So' o pacote offline (beta-package.json + sha256, 'BETA CONTROLADO - pacote offline verificado: <head>') prova o SHA."
  ],
  "physical_deltas": [
    "SOLVER: nenhum. nuvem/core/engine/* (16 arquivos versionados) com blobs identicos aos do PR #42 93f3885 (`git diff --quiet 93f3885 0641b5b -- nuvem/core/engine`).",
    "wall_modeling.py, comparacao por AST de cada definicao de topo e de cada metodo (93f3885 x 89384db): 0 removidas; 21 novas; 54 mudadas. Nenhuma funcao solve_*/auditoria de amarracao mudou. `controlled_beta_preflight` (o LAUDO da regra 48, nao o solver) mudou: abertura ativa por 0,1 cm em Z e sobreposicao 3D registrada. Mudaram tambem: create_building_blocks, _PostCreationEventHandler.__init__/_execute_create/_execute_create_batch/_execute_solve/_execute_delete, _PostCreationForm._update_delete_enabled, _format_block_solve_report e as classes/funcoes de UI do #46.",
    "MATERIALIZACAO (o que o Revit recebe) MUDOU: a peca que ocupa o volume real de abertura (ou colide) nao e' criada. Medido nas 46 paredes do usuario, 3 m, somente leitura: 8379 planejadas, 11 puladas (OPENING_VOID_INVASION), 8368 criaveis; conferencia pos-materializacao 0 invasao / 0 colisao / 0 erro.",
    "CORRECAO DESTE CAMPO: a versao anterior dizia 'regiao fisica de wall_modeling.py (linhas 1..9800) byte a byte igual a' do #42 - conferido por sha256 depois de cada commit'. FALSO desde aa70d0d: sha256 das linhas 1..9800 (LF) = e011a6182acf927f em 93f3885 e 0d7b090; f79f5854f601816d em aa70d0d e 09cfdd0; f7ebf634045452f5 em 0641b5b; 21349237391421ed em 89384db. O recorte por numero de linha tambem nao prova nada (insercoes deslocam linhas) - substituido pela comparacao por AST acima."
  ],
  "decisions_taken": [
    "Conflito de wall_modeling.py resolvido por auditoria simbolo a simbolo (nao por ours/theirs): fisica do #42, apresentacao do #46.",
    "MISSING_REQUIRED_JUNCTION_BOND aparece como REVISAO NECESSARIA (nunca falha); NO_FUNCTIONAL_JUNCTION so' como nota tecnica; microajuste so' vira 'ajustada' quando confirmado.",
    "OPCAO A (usuario): nenhuma peca ocupa porta; sem isencao para amarracao; amarracao pulada = NAO resolvida + revisao humana; FATAL bloqueia a RUN; violacao localizada nao volta a bloquear o lote inteiro.",
    "Regra 48, tolerancia em Z = 0,1 cm (a mesma da planta) no laudo; a de 0,5 cm fica so' no solver. Finalizar retem por parede em vez de travar a planta (secao 48 ja' previa a retencao de paredes incompletas)."
  ],
  "decisions_pending": [
    "Estender (ou nao) a regra 48 ao modo online sem beta - hoje ele cria e marca em vermelho (regra de 2026-08-26).",
    "Resolver as amarracoes NAO resolvidas do no' 41 (B34 T_INTERSECTION_DEGRADED_L a 0,5 cm dentro da porta de 8284539, fiadas 0-10) - revisao humana/projeto.",
    "Decidir o destino dos PRs #42 e #46 apos o merge do candidato.",
    "Integrar de fato a Etapa 3B ao botao exige mover aberturas no modelo do usuario: autorizacao explicita e movedor transacional novo."
  ],
  "next_steps": [
    "Parar depois do merge (instrucao do usuario). Proxima etapa, so' quando pedida: comparacao forense dos tres Revit (MCP=butanta testes, SCRIPT=TESTE PR49, HUMANO=BUTANTA R08_LT).",
    "Regerar o pacote offline do beta a partir da main mesclada antes de qualquer nova execucao pelo botao (o pacote antigo de 09cfdd0 tem a politica errada do aa70d0d)."
  ],
  "scope": "current"
}
```

## 1. O que foi integrado, e como

Base `origin/main` `55e990d`. Primeiro o **PR #42 inteiro** (`93f3885`) — `main` é ancestral, então
o merge não teve conflito e a árvore ficou **idêntica** à do #42. Depois o **PR #46** (`ae4586e0`).

A interseção real entre os dois PRs é de **três arquivos**: `nuvem/core/wall_modeling.py`,
`nuvem/REGRAS_MODULACAO_BLOCOS.md` e `docs/PROJECT_STATUS.md`. Os seis módulos novos da UI
(`ui_state`, `ui_chrome`, `ui_components`, `ui_execution`, `ui_native_style`, `ui_preview_panel`)
entraram sem conflito.

| arquivo | resolução |
|---|---|
| `nuvem/core/wall_modeling.py` | **automática, depois auditada**: no merge do #46 (`0d7b090`) o sha256 das linhas 1–9800 conferia com o #42 (`e011a618…`); **a partir de `aa70d0d` deixou de conferir** (ver §7). Os 68 hunks do #46 caem todos em classes/funções de UI (`_SetupForm`, `_WallReviewForm`, `_ProgressConsole`, `_PostCreationForm`, `run_modulation_on_existing_walls`, `main`, `_style_*`). Contra o #46, as únicas diferenças são funções do solver (até a linha 4865). |
| `nuvem/REGRAS_MODULACAO_BLOCOS.md` | automática: física do #42 (+2.044 linhas, seções 52–77) e seção UX do #46 (+50) coexistem |
| `docs/PROJECT_STATUS.md` | **conflito textual único, reconciliado à mão** (nunca `ours`/`theirs`): painel do #42, linhas de UI do #46 e a linha nova do candidato de integração |
| `nuvem/core/engine/*` | intocado pelo #46 — idêntico ao #42 |

## 2. Adaptador pós-#42 (o trabalho real da integração)

A UI do #46 foi escrita antes das regras 75/76/76.1/77 e não conhecia os desfechos novos. O
adaptador **só lê** o resultado do motor:

- `ui_state.hard_gate_counts` / `hard_gate_total`: colisão, fora do módulo, invasão de vão,
  canaleta e compensador exercendo amarração, peças sem apoio.
- `ui_state.review_items`: `MISSING_REQUIRED_JUNCTION_BOND` (regra 76.1, `review: HUMAN_REVIEW`)
  vira **REVISÃO NECESSÁRIA** — nunca "falha", nunca "tudo perfeito".
- `ui_state.no_functional_junction_count`: as fiadas da **seção 77** aparecem só como nota técnica.
- `ModulationUiState.completed(create_result, solve_result)`: com item de revisão o desfecho é
  **pendência**, não sucesso.
- `ui_execution`: estado `not_evaluated` do microajuste — o fluxo do botão **não** roda a Etapa 3B
  da seção 66, então a UI não pode afirmar "abertura ajustada" nem "ajuste não necessário".
- `wall_modeling` (só na região de UI): publica o snapshot que o #46 já esperava em
  `present_execution`, com `run_id`/`revision` — evento atrasado de uma análise antiga não
  sobrescreve a atual.

## 3. Prova do contrato com dados REAIS do BUTANTÃ

A UI foi renderizada em WinForms de verdade (offscreen) alimentada com o **resultado real do smoke
do #42** (lote `20260922-225600`): 8.693 peças, `MISSING` 3, `NO_FUNCTIONAL_JUNCTION` 7, portões 0.

A tela final mostra **"! Concluída com revisão necessária"**, "Revisão humana: 3 encontro(s) a
revisar", "Bloqueios críticos: 0", "Aberturas movidas: —" (desconhecido ≠ zero) e, no relatório,
o bloco `REVISÃO NECESSÁRIA (não é falha de criação)` com os três encontros dos nós 47/48 mais a
linha técnica das sete fiadas sem encontro funcional.

## 4. Instalação verificada dentro do Revit

O botão já roda em **BETA CONTROLADO** (pacote offline, sem download). O pacote foi regerado a
partir desta branch e **verificado pelo verificador do próprio loader, de dentro do Revit**:
`head = 48d3914…`, 25 módulos em `core/` (incluindo os seis de UI), entry
`…/teste-perf.pushbutton/core/wall_modeling.py`. O guarda de evento velho
(`run_id`/`revision`) foi exercitado no mesmo teste: aceita a revisão nova, recusa a repetida e
recusa a execução antiga.

## 5. Pendência

**O smoke do BOTÃO REAL depende de um clique humano** — a primeira tela é modal e o Revit está
aberto na máquina do usuário. Tudo o que não depende do clique está provado: pacote instalado e
verificado no Revit, solver idêntico ao #42 (engine/ blob-idêntico), UI do #46 preservada, contrato ligado e testado.


## 6. Correção da regressão do fluxo do botão (2026-09-23, tarde)

Execução real do usuário pelo botão (46 paredes do modelo, altura 3 m): a Etapa 1 "sumia", a Etapa 4
mostrava 8.332 blocos com 371 ocorrências e **a criação inteira bloqueada**, e a UI dizia "Microajuste
não avaliado (Etapa 3B fora do fluxo)".

**Reprodução offline (somente leitura, no modelo real):** 46 paredes, 3 m → **8.379 peças**,
`preflight_ok=False`, **11 invasões de vão**, 0 colisões, `non_modular` 96 concentradas em **4 paredes**
(8284526 48, 8284502 20, 8284546 14, 8284539 14), `door_void` 12 em **1 parede** (8284539),
`unsupported` 30. As 11 invasões são **um único caso físico**: nó 41, mesma abertura, 11 fiadas, sempre
a **B34 de amarração** (`T_INTERSECTION_DEGRADED_L`) invadindo **0,5 cm**.

| causa | onde | correção |
|---|---|---|
| configuração sem dono | Etapa 1 é modal que devolve um valor; a camada premium dá `Controls.Clear()` no form do host | `setup` por execução em `handler.setup`, entregue por kwarg e **re-renderizado a partir do estado** (`_ui_config`); persistência própria do fluxo de paredes existentes |
| criação bloqueada | `controlled_beta_preflight` é gate de lote; `_execute_create` levantava `BETA BLOQUEADO` | classificação por peça a partir do **mesmo laudo** — política **substituída pela OPÇÃO A (§7)**: a versão desta linha dizia que a amarração que viola seria criada, o que viola a regra 48 |
| Etapa 3B | `plan_opening_micro_adjustments` nunca teve chamador | estado explícito e testável (`MICRO_ADJUST_IN_BUTTON_FLOW`, `micro_adjust_flow_state`) com o motivo no relatório |

**Efeito medido na mesma geometria (política de `aa70d0d`, SUPERADA):** criaria as 11 B34 dentro da porta. Ver §7 para a medição da OPÇÃO A.

**Regra 75 preservada:** canaleta continua proibida como amarração — o gate é auditoria independente e
há teste de regressão cobrindo isso.

## 7. OPÇÃO A — regra 48 sem exceção (2026-09-23, decisão do usuário)

A política de materialização de `aa70d0d` **violava a regra 48**: criava a peça de amarração que
invadia a porta (isenção por código B34/B54 e até pelo compensador da 76.1) e o checkpoint afirmava
"byte a byte" sem prova. O usuário escolheu a **OPÇÃO A**; o princípio da regra 48 não mudou, só a
consequência operacional:

| situação | consequência |
|---|---|
| peça (qualquer tipo) ocupa o volume real de abertura > 0,1 cm | **não é criada** (`OPENING_VOID_INVASION`) |
| duas peças colidem | uma não é criada (`PIECE_COLLISION`); se só uma for amarração, a outra sai |
| a peça não criada era amarração (papel do solver + B34/B54) | nó/fiada vira **`BOND_UNRESOLVED`** — revisão humana obrigatória |
| erro FATAL do plano | a RUN inteira continua bloqueada, nada é tocado |
| sobra invasão/colisão depois de separar as peças (`verify_materialization`) | FATAL — nunca criação parcial |

**Papel de amarração** (`structural_bond_role`): razão de encontro gravada pelo solver
(`L_CORNER`/`T_INTERSECTION`/`X_INTERSECTION`/`CORNER`) **e** código B34/B54. B34/B54 de
preenchimento não é amarração; `JUNCTION_UNRESOLVED_FILL` (76.1) e canaleta (75) nunca são.

### Medição no modelo real (somente leitura, MCP, sem Transaction)

`butanta testes`, 46 paredes, 3 m, CHANNEL — documento escolhido pelo título, sem trocar a janela
ativa do usuário; `IsModifiable=False` do início ao fim.

| | |
|---|---|
| planejadas | **8.379** |
| puladas | **11** (todas `OPENING_VOID_INVASION`) |
| criáveis | **8.368** — planejadas = criáveis + puladas (fecha) |
| erro fatal | 0 |
| conferência pós-materialização (`verify_materialization`) | **0 invasão, 0 colisão, 0 erro** |
| força bruta (toda peça criável × toda abertura de toda parede, por fiada, SEM tolerância em Z) | 0 peça em abertura ativa; 17 vergas de canaleta na fiada 11 (221–240 cm) sobre vergas de porta/janela a 221,0 cm: sobreposição vertical **3×10⁻¹² cm** (ruído de ponto flutuante — encostam, não ocupam) |
| `door_void_violations` do solver | 12 registros = as mesmas B34 do nó 41; **nenhum materializado** |

**As 11 B34 do nó 41:** todas `T_INTERSECTION_DEGRADED_L`, papel de amarração **real** dado pelo
solver (nenhuma classificada só pelo código), todas **puladas**, sobreposição 0,49995 cm na porta 1
da parede 8284539 (314–610 cm, 0–221 cm). Paredes 8284539 (fiadas 0, 2, 4, 6, 8, 10) e 8284569
(fiadas 1, 3, 5, 7, 9). Resultado: **nó 41, fiadas 0–10 = AMARRAÇÃO NÃO RESOLVIDA** (revisão humana).
Independentemente disso, o solver já marcava `MISSING_REQUIRED_JUNCTION_BOND` em 18 fiadas dos nós
1, 22, 44, 48, 71 e 72 (regra 76.1, inalterado). **RUN executável: sim. Modulação estruturalmente
resolvida: NÃO.**

**Canaleta como amarração:** `channel_as_junction_bond` (regra 75) não foi tocado; a
materialização nunca dá papel de amarração a canaleta; testes cobrem os dois lados.

### Auditoria independente da OPÇÃO A (`0641b5b` → `89384db`)

Três auditores independentes (regra 48; critério de amarração/76.1/canaleta; contabilidade/estado/UI), cada
achado tentado refutar por um verificador com reprodução. **13 achados, todos reproduzidos, todos corrigidos**:

| achado | correção |
|---|---|
| o laudo da regra 48 usava a tolerância de ruído de **0,5 cm em Z** do solver: até 5 mm de peça dentro do vão sem registro (verga a 221,4 cm × fiada a partir de 221 cm) | abertura ativa por **0,1 cm** também em Z; sobreposição registrada = real em 3D; o solver não muda |
| colisão com peça **já pulada** pulava uma peça válida e criava amarração NÃO resolvida falsa (2 auditores) | colisão cuja outra peça já saiu está resolvida |
| **Finalizar** travado para a planta inteira por pulo localizado (2 auditores) | parede afetada retida (`INCOMPLETE_CREATION`); fatal continua bloqueando — backend e botão |
| chave física repetida: pular uma pulava as duas; contagem divergente (2) | pulo por **identidade** da peça |
| registro sem a parede da abertura | `opening_wall_idx` no pulo e na pendência |
| retorno antecipado sem contabilidade; "None"/"fecha: NÃO" para desconhecido | fecha também ali; desconhecido = "—" |
| relatório: "RUN executada: não" com 0 criadas; pendências duplicadas; retenção da criação ignorada | corrigidos |

Medição no modelo real repetida depois das correções: **mesmos números** (8.379 / 11 / 8.368; 0 peça em abertura).
